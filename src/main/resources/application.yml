spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/webapp
    username: postgres
    password: 2004

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
